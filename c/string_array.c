#include <stdio.h>

/* 确定一个字符串中的所有字符是否全都不同 */
int isUniqueChars(char *str)
{
    int i;
    char flag[128] = {0,};

    if( str == NULL) return -1;

    for(i=0; a[i]!='\0', i++){
        
        
    }
}

/* 反转一个已null结尾的字符串*/
void reverse(char *str)
{
}


